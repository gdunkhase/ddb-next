<g:each in="${binaryList}">
  <div>preview title ---> <span>${it.preview.title}</span></div>
  <div>preview uri ---> <span>${it.preview.uri}</span></div>
  <div>thumbnail title ---> <span>${it.thumbnail.title}</span></div>
  <div>thumbnail uri ---> <span>${it.thumbnail.uri}</span></div>
  <div>full uri ---> <span>${it.full.uri}</span></div>
</g:each>

