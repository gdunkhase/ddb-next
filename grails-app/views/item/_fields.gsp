<!-- TODO localization -->
<div class="fields">

<g:each in="${fields}">
  <div class="row">
    <div class="name span3">${it.name}</div>
    <div class="value span5">${it.value}</div>
  </div>
</g:each>

</div>
